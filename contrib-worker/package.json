{
  "name": "avatarian-contrib-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that turns an Avatarian corpus submission into a pull request.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
