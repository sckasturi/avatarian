/* Avatarian gadget styling — paste into MediaWiki:Common.css
   Syllable blocks: vowel underneath the consonant, packed tight
   (mirrors canon reference art). Both rows are the same size — the
   glyphs share one 100x100 grid, so scaling the lower row down shrinks
   the whole mark rather than just its band.
   Sized in em so Avatarian scales with the surrounding wiki text,
   and coloured via currentColor so it matches link/body text. */
.avatarian-word { display: inline-flex; align-items: flex-start; vertical-align: middle; margin: 0 0.3em 0 0; }
.avatarian-syllable { display: inline-flex; flex-direction: column; align-items: center; margin-right: 0.05em; }
.avatarian-onset { display: flex; align-items: flex-end; gap: 0.02em; }
.avatarian-nucleus { display: flex; justify-content: center; height: 1.25em; }
.avatarian-glyph { display: inline-block; line-height: 0; color: currentColor; }
.avatarian-glyph svg { display: block; width: 100%; height: 100%; }
.avatarian-glyph.avatarian-consonant { width: 1.25em; height: 1.25em; }
.avatarian-glyph.avatarian-vowel { width: 1.25em; height: 1.25em; }
.avatarian-placeholder { opacity: 0.5; }
